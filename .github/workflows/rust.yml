name: Rust

on:
  push:
    branches: [ "main", "develop" ]
  pull_request:
    branches: [ "main", "develop" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Show Rust toolchain
      run: rustup show active-toolchain
    - name: Check formatting
      run: cargo fmt --all -- --check
    - name: Run clippy
      run: cargo clippy --workspace --all-targets -- -D warnings
    - name: Build
      run: cargo build --verbose
    - name: Run tests
      env:
        COGNATE_REQUIRE_SOCKET_TESTS: "1"
      run: cargo test --workspace --verbose -- --nocapture --test-threads=1

  reliability:
    name: Reliability tests
    runs-on: ubuntu-latest
    needs: build
    steps:
    - uses: actions/checkout@v4
    - name: Show Rust toolchain
      run: rustup show active-toolchain
    - name: Run transaction, recovery, and search consistency tests
      run: cargo test -p cognate-engine --test notebook_tests --test search_tests --verbose -- --nocapture --test-threads=1
